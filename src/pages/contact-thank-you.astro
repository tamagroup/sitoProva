---
import Layout from '../layouts/Page.astro';
import { Container } from '@components/odyssey-theme';

const seo = {
	title: 'Messaggio Inviato | SkyMetric',
	description: 'Grazie per averci contattato. Ti risponderemo presto!',
};
---

<script>
	// Fallback per assicurarsi che la pagina funzioni anche se il redirect non avviene
	if (typeof window !== 'undefined') {
		// Mostra un messaggio di conferma dopo 1 secondo
		setTimeout(() => {
			const content = document.querySelector('.thank-you__content');
			if (content) {
				content.style.opacity = '1';
				content.style.transform = 'translateY(0)';
			}
		}, 100);
	}
</script>

<Layout seo={seo}>
	<Container>
		<section class="thank-you__section">
			<div class="thank-you__content">
				<h1>Messaggio Inviato!</h1>
				<p>
					Grazie per averci contattato. Abbiamo ricevuto il tuo messaggio e ti risponderemo al più presto possibile.
				</p>
				<p>
					Se hai bisogno di assistenza immediata, puoi contattarci direttamente:
				</p>
				<div class="contact-info">
					<p><strong>Telefono:</strong> <a href="tel:3520808083">3520808083</a></p>
					<p><strong>Email:</strong> <a href="mailto:info@skymetric.it">info@skymetric.it</a></p>
					<p><strong>WhatsApp:</strong> <a href="http://wa.me/393520808083">Contattaci su WhatsApp</a></p>
				</div>
				<div class="thank-you__actions">
					<a href="/" class="btn btn--primary">Torna alla Home</a>
					<a href="/company/contact" class="btn btn--outlined">Invia un altro messaggio</a>
				</div>
			</div>
		</section>
	</Container>
</Layout>

<style>
	.thank-you__section {
		margin: var(--section-margin) auto;
		min-height: calc(90vh - var(--navbar-height));
		display: flex;
		align-items: center;
		justify-content: center;
	}
	.thank-you__content {
		text-align: center;
		max-width: 600px;
		padding: 2rem;
		background-color: var(--theme-surface-1);
		border-radius: var(--theme-shape-radius);
		opacity: 0;
		transform: translateY(20px);
		transition: opacity 0.6s ease, transform 0.6s ease;
	}
	.thank-you__content h1 {
		color: var(--theme-primary);
		margin-bottom: 1rem;
	}
	.contact-info {
		margin: 2rem 0;
		padding: 1rem;
		background-color: var(--theme-surface-2);
		border-radius: var(--theme-shape-radius);
	}
	.contact-info p {
		margin: 0.5rem 0;
	}
	.contact-info a {
		color: var(--theme-primary);
		text-decoration: none;
	}
	.contact-info a:hover {
		text-decoration: underline;
	}
	.thank-you__actions {
		display: flex;
		gap: 1rem;
		justify-content: center;
		margin-top: 2rem;
	}
	.btn {
		padding: 0.75rem 1.5rem;
		border-radius: var(--theme-button-border-radius);
		text-decoration: none;
		font-weight: 600;
		transition: var(--theme-transition);
		display: inline-block;
	}
	.btn--primary {
		background-color: var(--theme-primary);
		color: var(--theme-on-primary);
	}
	.btn--primary:hover {
		background-color: var(--theme-primary-hover);
	}
	.btn--outlined {
		border: 2px solid var(--theme-primary);
		color: var(--theme-primary);
		background-color: transparent;
	}
	.btn--outlined:hover {
		background-color: var(--theme-primary);
		color: var(--theme-on-primary);
	}
	@media (max-width: 768px) {
		.thank-you__section {
			padding: 1rem;
			min-height: calc(100vh - var(--navbar-height));
		}
		.thank-you__content {
			padding: 1.5rem;
			margin: 1rem 0;
		}
		.thank-you__content h1 {
			font-size: clamp(1.5rem, 5vw, 2.2rem);
		}
		.thank-you__actions {
			flex-direction: column;
			align-items: center;
			gap: 0.75rem;
		}
		.btn {
			width: 100%;
			max-width: 280px;
			padding: 0.875rem 1.25rem;
		}
		.contact-info {
			margin: 1.5rem 0;
			padding: 1rem;
		}
		.contact-info p {
			font-size: 0.9rem;
		}
	}
	@media (max-width: 480px) {
		.thank-you__content {
			padding: 1rem;
		}
		.thank-you__content h1 {
			font-size: 1.6rem;
		}
		.contact-info p {
			font-size: 0.85rem;
		}
	}
</style>
